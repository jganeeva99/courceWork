#include <stdio.h>
using namespace std;

void SayPassword(string val){
    cout << val << endl;
}

int main ()
{
    char a ="%d%d%d"; // ошиб форм стр
    int b;
    double c;
    cout<<"What can i type"<<endl;
    printf("%d%s",b,a;
           
    string s = ""; // внедр ком
    std::cin >> s;
    _wsystem(s);
           
    string l ="%u%x%p"; // ошиб форм стр
    int l =200;
    printf (str, "%s%d %d",str, l,l);
           
    char buf[]; // неиниц перем
    for (int i = 0; i < 10; ++i)
        cout << buf[i] << " ";
           
    string crypt = "qwerty"; // пренебр без хран
    SayPassword(crypt);
           
    int y = 10000000000; // переполн
    while (true){
        cout << "test" << endl;
        y = y + 5009000000000;
    }
           
    return 0;
}

